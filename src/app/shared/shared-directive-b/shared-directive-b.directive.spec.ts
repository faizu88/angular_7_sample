import { SharedDirectiveBDirective } from './shared-directive-b.directive';

describe('SharedDirectiveBDirective', () => {
  it('should create an instance', () => {
    const directive = new SharedDirectiveBDirective();
    expect(directive).toBeTruthy();
  });
});
