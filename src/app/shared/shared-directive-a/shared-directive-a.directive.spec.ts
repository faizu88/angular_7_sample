import { SharedDirectiveADirective } from './shared-directive-a.directive';

describe('SharedDirectiveADirective', () => {
  it('should create an instance', () => {
    const directive = new SharedDirectiveADirective();
    expect(directive).toBeTruthy();
  });
});
