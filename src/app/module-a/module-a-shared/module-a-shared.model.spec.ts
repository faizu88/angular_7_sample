import { ModuleAShared.Model } from './module-a-shared.model';

describe('ModuleAShared.Model', () => {
  it('should create an instance', () => {
    expect(new ModuleAShared.Model()).toBeTruthy();
  });
});
